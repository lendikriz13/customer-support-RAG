# API Keys (Required)
# Get OpenAI key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxx

# Get Anthropic key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx

# Document Processing Settings
CHUNK_SIZE=1000                    # Characters per chunk (500-1500 recommended)
CHUNK_OVERLAP=100                  # Overlap between chunks (helps with context continuity)

# Model Configuration
EMBEDDING_MODEL=text-embedding-3-small    # OpenAI embedding model (cheap & fast)
LLM_MODEL=claude-sonnet-4-20250514        # Claude model for responses

# RAG Settings
RETRIEVAL_K=3                      # Number of document chunks to retrieve per query
TEMPERATURE=0.3                    # LLM creativity (0=focused, 1=creative)

# Optional: Debug Mode
DEBUG=false                        # Set to 'true' to see retrieved chunks in console
